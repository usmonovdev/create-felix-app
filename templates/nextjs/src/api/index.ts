import { POSTS, base_url, image_base_url } from "./URLs";

import { GET, POST, DELETE, EDIT } from "./config";

const Api = {
  GET,
  POST,
  DELETE,
  EDIT,
};

export default Api;

export const URLs = {
  POSTS,
  base_url,
  image_base_url,
};
